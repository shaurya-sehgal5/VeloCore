global:
  scrape_interval: 5s # High-resolution scrape loop for real-time demo updates

scrape_configs:
  - job_name: 'velocore-backend'
    static_configs:
      - targets: ['host.docker.internal:6000'] # Points straight back to your host Windows machine port
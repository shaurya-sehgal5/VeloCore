apiVersion: v1
kind: Service

metadata:
  name: grafana
  namespace: {{ .Values.namespace }}

spec:
  type: NodePort

  selector:
    app: grafana

  ports:
    - port: 3000
      targetPort: 3001
      nodePort: 30030
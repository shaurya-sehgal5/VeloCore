apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:

  name: velocore

  namespace: {{ .Values.namespace }}

spec:

  podSelector: {}

  policyTypes:

    - Ingress

    - Egress

  ingress:

    - {}

  egress:

    - {}